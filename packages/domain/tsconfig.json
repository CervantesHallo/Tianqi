{
  "//": "Phase 9 / Step 15 — §4.8 编译期硬约束（domain 不依赖 Port）。references 故意不含 ../ports：domain 内任何 import @tianqi/ports / 相对路径 ports / *-port 文件都会在 typecheck 期解析失败（CI 强制保证）。ESLint no-restricted-imports（packages/domain/**/*.ts glob，错误信息含 §4.8 引用）提供 IDE 即时红线（开发时即时反馈）。两机制双重保护 §4.8。元规则 B 锁定（自 Step 15 起）：本 references 数组任何调整必须经 ADR-0002 修订流程。详见 docs/decisions/0002-phase-9-saga-orchestration.md Step 15 段 + docs/phase9/15-compile-time-constraints.md。",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "rootDir": "src",
    "outDir": "dist"
  },
  "references": [{ "path": "../shared" }, { "path": "../contracts" }],
  "include": ["src/**/*.ts"]
}
